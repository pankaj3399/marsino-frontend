export interface BREADCRUMB_INTERFACE {
  label: string;
  path: string;
}

export interface ReportType {
  car_insurance_id: string;
  last_name: string;
  first_name: string;
  dob: string;
  street: string;
  city: string;
  postal_code: string;
}
